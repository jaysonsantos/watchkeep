{
  "db_name": "PostgreSQL",
  "query": "WITH bounds AS (\n             SELECT date_trunc('month', $2 AT TIME ZONE $1) AS last_month\n           ), series AS (\n             SELECT generate_series(last_month - make_interval(months => $3), last_month, interval '1 month') AS start\n             FROM bounds\n           )\n           SELECT to_char(s.start, 'YYYY-MM') AS \"period!\",\n                  COUNT(d.id) AS \"plays!\",\n                  COALESCE(SUM(d.runtime_ms), 0)::bigint AS \"runtime_ms!\"\n           FROM series s\n           LEFT JOIN play_details d ON date_trunc('month', d.watched_at AT TIME ZONE $1) = s.start\n           GROUP BY s.start\n           ORDER BY s.start",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "period!",
        "type_info": "Text",
        "origin": "Expression"
      },
      {
        "ordinal": 1,
        "name": "plays!",
        "type_info": "Int8",
        "origin": "Expression"
      },
      {
        "ordinal": 2,
        "name": "runtime_ms!",
        "type_info": "Int8",
        "origin": "Expression"
      }
    ],
    "parameters": {
      "Left": [
        "Text",
        "Timestamptz",
        "Int4"
      ]
    },
    "nullable": [
      null,
      null,
      null
    ]
  },
  "hash": "3533cb21cab81972c2f0db55b72678972f95752c587b1fd9b68617ff7bbea13c"
}
